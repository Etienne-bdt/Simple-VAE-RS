name: vae-rs
channels: 
  - conda-forge
  - defaults
dependencies:
  - numpy
  - rasterio
  - tqdm
  - matplotlib
  - polars
  - pre-commit
  - pip
  - pip:
      - torch
      - tensorboard
      - standard-imghdr
