name: vae-rs
channels: 
  - conda-forge
  - defaults
dependencies:
  - numpy
  - rasterio
  - tqdm
  - matplotlib
  - polars
  - pip
  - pip:
      - torch
      - tensorboard
      - standard-imghdr
